interface InitailState {}

const initailState: InitailState = {};

const ComponentsDataReducer = (state = initailState, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default ComponentsDataReducer;
